Audit + Refactor tasks (GradeINT)
Session rule: Option B (multiple sessions allowed per exam).
Submission rule: Rule 1 (a student can submit only once per session): enforce unique(studentId, sessionId).

Implement the following changes with minimal churn. Prioritize correctness and data integrity first. Provide diffs for each file you touch.

1) Enforce exam immutability after publish
- Add/confirm exam.status enum: "draft" | "published".
- Block edits to exam metadata and questions when exam.status === "published" (update exam, add/edit/delete questions). Return 400 with clear message.
- Draft exams remain fully editable.

2) Add transactions for multi-step DB writes
Wrap these in a DB transaction (rollback on failure):
- createExam (insert exam + bulk insert questions if provided)
- updateExam (update exam + question delete/replace, only allowed in draft)
- publishExam (validate question count, set status)
- deleteExam (delete questions + sessions + submissions + exam to avoid orphans)

3) Publish should not auto-create sessions (Option B)
- Publishing an exam should ONLY change status to "published" after validating it has >= 1 question.
- Remove any behavior that automatically creates an exam session inside publishExam/publish route.
- Sessions are created explicitly via the sessions endpoint with its own validation.

4) Guard session creation for integrity
In POST /api/sessions (or equivalent):
- Verify exam exists
- Verify exam.status === "published"
- Verify questionCount >= 1
- Then create a new session (multiple sessions allowed)
- Return 400 with clear message if any validation fails.

5) Enforce submission uniqueness + remove N+1 checks
- Replace any “fetch all submissions then scan” logic with a direct query for duplicates:
  getSubmissionByStudentAndSession(studentId, sessionId) (create this helper if missing).
- Add DB unique constraint: unique(studentId, sessionId).
- In POST /api/submissions:
  - Check duplicates with that direct query, or rely on DB unique violation handling in a transaction.
  - If duplicate, return 409 (preferred) or 400 with message "Submission already exists for this session".

6) Ensure cascade deletes / no orphan records
- On deleteExam, delete related questions, sessions, submissions in one transaction OR add FK cascade constraints.
- Ensure no orphan sessions/submissions referencing a deleted exam.

7) Validate all write endpoints with shared Zod schemas
- For POST/PUT/PATCH endpoints, parse req.body with matching schema (api.*.input).
- Return 400 with validation details.

8) React Query consistency + remove duplicate hooks
- Standardize query keys:
  - questions key must be ["examQuestions", examId]
- Ensure mutations invalidate the exact matching key.
- Remove duplicate useCreateSubmission hook (currently duplicated); keep one canonical hook and update imports/usages.

Output
- Summarize each change and list exact files modified.
- Keep behavior unchanged except where required above.
- Add 2-3 small smoke tests (manual steps) in the summary for me to verify quickly.