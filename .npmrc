# pnpm configuration for GradeINT monorepo
# Hoist Vite's internal transitive dependencies so server code can import them
public-hoist-pattern[]=nanoid
public-hoist-pattern[]=esbuild
public-hoist-pattern[]=rollup
